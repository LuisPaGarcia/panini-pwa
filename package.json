{
  "name": "pwa-panini",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.19.0",
    "idb": "^2.1.1",
    "now": "^11.0.3",
    "react": "^16.3.0",
    "react-dom": "^16.3.0",
    "react-helmet": "^5.2.0",
    "react-scripts": "1.1.4",
    "styled-components": "^3.2.5",
    "sweetalert2": "^7.18.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy-netlify": "yarn build && netlify deploy ./build",
    "deploy-now": "yarn build && cd build/ && now --public",
    "deploy-all": "yarn netlify && yarn now"
  }
}
